$(document).ready(function(){
	observeLinks();
	
});
// ########################################################
function ajaxLoad(){
	jQuery.get('index.php',{id: 109},function(data){alert(data);});
}
	
function observeLinks()
{
	// verweise mit ajax verarbeiten###########################
	var ajaxLink;

	$("option[onclick]").attr("onclick", "AJAX");// onclick inaktiv
			
	// submit-formulare----------------------------------------
	$("input[type='submit']").click(function() { 		
		ajaxLoad();	
		return false;
	});		
	
	// onclick-formulare---------------------------------------
	$("option[onclick]").click(function() { 
		ajaxLoad();	
		return false;		
	});
	
	// <a href>------------------------------------------------
	$("a").click(function(){		

		ajaxLoad();
		return false;	
	});
}

